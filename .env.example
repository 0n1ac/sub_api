# 서버 설정
HOST=127.0.0.1
PORT=8000

# CLI 응답 대기 시간(초)
TIMEOUT=60

# 서버 모드에서 같은 backend를 동시에 몇 개까지 실행할지 설정
SUB_API_SERVER_MAX_CONCURRENT_PER_BACKEND=1

# concurrency slot을 기다릴 최대 시간(초). 비우면 요청 timeout을 사용
# SUB_API_CONCURRENCY_QUEUE_TIMEOUT=30

# model이 생략됐을 때 사용할 백엔드: gemini, claude, codex
DEFAULT_BACKEND=gemini

# 백엔드별 세부 모델 기본값 (비워두면 각 CLI의 native default 사용)
# SUB_API_DEFAULT_MODEL_GEMINI=gemini-2.5-pro
# SUB_API_DEFAULT_MODEL_CLAUDE=sonnet
# SUB_API_DEFAULT_MODEL_CODEX=gpt-5
